<Page x:Class="GameClient.Views.ScoreboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:props="clr-namespace:GameClient.Resources"
      xmlns:fa="clr-namespace:FontAwesome.WPF;assembly=FontAwesome.WPF"
      mc:Ignorable="d" 
      d:DesignHeight="1080" d:DesignWidth="1920"
      Title="ScoreboardPage"
      FontFamily="/Assets/Fonts/#Dosis">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/fondo2.png" Stretch="UniformToFill" Opacity="1">
                <ImageBrush.Viewbox>
                    <Rect>0.2, 0.2, 0.6, 0.6</Rect>
                </ImageBrush.Viewbox>
            </ImageBrush>
        </Grid.Background>

        <Border Style="{StaticResource LobbyContainerBorderStyle}" Width="1000" Height="850">
            <Grid Margin="40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="{x:Static props:Strings.ScoreboardTitle}" 
                               FontSize="48" FontWeight="Bold" 
                               HorizontalAlignment="Left" VerticalAlignment="Center"/>

                    <Image Grid.Column="1" Source="/Assets/Icons/trophy_icon.png" Height="60"/>
                </Grid>

                <Border Grid.Row="1" Background="White" CornerRadius="10" BorderBrush="#DDD" BorderThickness="1" Padding="5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="50"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Background="#F5F5F5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="150"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{x:Static props:Strings.ScoreboardRank}" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            <TextBlock Grid.Column="2" Text="{x:Static props:Strings.ScoreboardPlayer}" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Margin="20,0"/>
                            <TextBlock Grid.Column="3" Text="{x:Static props:Strings.ScoreboardWins}" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Grid>

                        <ListBox x:Name="LeaderboardList" Grid.Row="1" 
                                 Background="Transparent" BorderThickness="0" 
                                 HorizontalContentAlignment="Stretch"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="0,2"/>
                                    <Setter Property="Cursor" Value="Arrow"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="ItemBorder" Background="Transparent" CornerRadius="5" Padding="5" BorderThickness="1" BorderBrush="Transparent">
                                                    <ContentPresenter/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <DataTrigger Binding="{Binding IsCurrentUser}" Value="True">
                                                        <Setter TargetName="ItemBorder" Property="Background" Value="#E3F2FD"/>
                                                        <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#2196F3"/>
                                                    </DataTrigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>

                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="150"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Text="{Binding Rank}" 
                                                   FontSize="24" FontWeight="Bold" Foreground="#555"
                                                   VerticalAlignment="Center" HorizontalAlignment="Center"/>

                                        <Grid Grid.Column="1" Width="50" Height="50">
                                            <Ellipse Fill="#EEE"/>
                                            <Image Source="{Binding AvatarPath, TargetNullValue=/Assets/Icons/profile_icon.png, FallbackValue=/Assets/Icons/profile_icon.png}" 
                                             Stretch="Uniform"/>
                                        </Grid>

                                        <TextBlock Grid.Column="2" Text="{Binding Username}" 
                                                   FontSize="22" FontWeight="SemiBold" VerticalAlignment="Center" Margin="20,0"/>

                                        <Border Grid.Column="3" Background="#FFD700" CornerRadius="15" 
                                                Padding="15,5" HorizontalAlignment="Center" MinWidth="80">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <TextBlock Text="{Binding Wins}" Foreground="White" FontWeight="Bold" FontSize="18"/>
                                                <fa:ImageAwesome Icon="Trophy" Foreground="White" Height="14" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
                    <Button x:Name="BackButton" Content="{x:Static props:Strings.BackButton}" 
                            Style="{StaticResource PrimaryButtonStyle}" 
                            Width="250" Height="60" 
                            Click="BackButtonClick"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>