<Page x:Class="GameClient.Views.JoinMatchCodePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:GameClient.Views"
      xmlns:props="clr-namespace:GameClient.Resources"
      mc:Ignorable="d" 
      d:DesignHeight="1080" d:DesignWidth="1920"
      Title="JoinMatchCodePage">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/fondo2.png" 
                        Stretch="UniformToFill" 
                        Opacity="1">
                <ImageBrush.Viewbox>
                    <Rect>0.2, 0.2, 0.6, 0.6</Rect>
                </ImageBrush.Viewbox>
            </ImageBrush>
        </Grid.Background>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,50,0,0">

            <TextBlock Text="{x:Static props:Strings.JoinMatchCodeTitle}" 
                       FontFamily="/Assets/Fonts/#Dosis" 
                       FontSize="48" 
                       FontWeight="Bold" 
                       Foreground="Black" 
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <TextBox x:Name="LobbyCodeBox"
                     Width="400"
                     Height="60"
                     FontSize="36"
                     FontFamily="/Assets/Fonts/#Dosis"
                     FontWeight="Bold"
                     HorizontalContentAlignment="Center"
                     VerticalContentAlignment="Center"
                     MaxLength="5"
                     CharacterCasing="Upper"
                     Margin="0,0,0,30"/>

            <Button x:Name="JoinButton" 
                    Content="{x:Static props:Strings.JoinButton}" 
                    Style="{StaticResource PrimaryButtonStyle}" 
                    Width="300" 
                    Height="80"
                    FontFamily="/Assets/Fonts/#Dosis" 
                    FontSize="36" 
                    FontWeight="Bold"
                    Click="JoinButton_Click"/>

        </StackPanel>

        <Button x:Name="BackButton" 
                Content="{x:Static props:Strings.BackButton}"
                Style="{StaticResource PrimaryButtonStyle}"
                FontFamily="/Assets/Fonts/#Dosis" 
                FontSize="24"
                FontWeight="Bold"
                HorizontalAlignment="Left" 
                VerticalAlignment="Top" 
                Margin="20" 
                Width="100" 
                Height="50"
                Cursor="Hand" 
                Click="BackButton_Click"/>

    </Grid>
</Page>