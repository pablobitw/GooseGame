<Page x:Class="GameClient.Views.ListMatchesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:props="clr-namespace:GameClient.Resources"
      xmlns:fa="clr-namespace:FontAwesome.WPF;assembly=FontAwesome.WPF"
      mc:Ignorable="d" 
      d:DesignHeight="1080" d:DesignWidth="1920"
      Title="ListMatchesPage"
      FontFamily="/Assets/Fonts/#Dosis">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/fondo2.png" Stretch="UniformToFill" Opacity="1">
                <ImageBrush.Viewbox>
                    <Rect>0.2, 0.2, 0.6, 0.6</Rect>
                </ImageBrush.Viewbox>
            </ImageBrush>
        </Grid.Background>

        <Border Style="{StaticResource LobbyContainerBorderStyle}" Width="1200" Height="850">
            <Grid Margin="40">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="{x:Static props:Strings.PublicMatchesTitle}" 
                               FontSize="48" FontWeight="Bold" 
                               HorizontalAlignment="Left" VerticalAlignment="Center"/>

                    <Button x:Name="RefreshButton" Grid.Column="1" 
                            Style="{StaticResource OrangeButtonStyle}" 
                            Width="180" Height="50" FontSize="20"
                            Click="RefreshButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <fa:ImageAwesome Icon="Refresh" Foreground="White" Height="20" Margin="0,0,10,0"/>
                            <TextBlock Text="{x:Static props:Strings.RefreshButton}"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <Border Grid.Row="1" Background="White" CornerRadius="10" BorderBrush="#DDD" BorderThickness="1" Padding="5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="50"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Background="#F5F5F5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{x:Static props:Strings.HostHeader}" 
                                       FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Margin="20,0"/>
                            <TextBlock Grid.Column="1" Text="{x:Static props:Strings.BoardHeader}" 
                                       FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Margin="10,0"/>
                            <TextBlock Grid.Column="2" Text="{x:Static props:Strings.PlayersHeader}" 
                                       FontSize="20" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Grid>

                        <ListBox x:Name="MatchesListBox" Grid.Row="1" 
                                 Background="Transparent" BorderThickness="0" 
                                 HorizontalContentAlignment="Stretch"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="0,2"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="ItemBorder" Background="Transparent" CornerRadius="5" Padding="10" BorderThickness="1" BorderBrush="Transparent">
                                                    <ContentPresenter/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="ItemBorder" Property="Background" Value="#E0F7FA"/>
                                                        <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ItemBorder" Property="Background" Value="#FAFAFA"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>

                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="1*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                            <fa:ImageAwesome Icon="User" Foreground="Gray" Height="20" Margin="10,0,10,0"/>
                                            <TextBlock Text="{Binding HostName}" FontSize="22" FontWeight="SemiBold"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                            <fa:ImageAwesome Icon="Map" Foreground="Gray" Height="18" Margin="0,0,10,0"/>
                                            <TextBlock Text="{Binding MapName}" FontSize="20"/>
                                        </StackPanel>

                                        <Border Grid.Column="2" Background="{StaticResource PrimaryBrush}" CornerRadius="15" 
                                                Padding="15,5" HorizontalAlignment="Center" MinWidth="80">
                                            <TextBlock Text="{Binding PlayerCount}" Foreground="White" FontWeight="Bold" FontSize="18" HorizontalAlignment="Center"/>
                                        </Border>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <TextBlock x:Name="NoMatchesText" Grid.Row="1" 
                                   Text="{x:Static props:Strings.NoMatchesLabel}" 
                                   FontSize="24" Foreground="Gray" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center" 
                                   Visibility="Collapsed"/>
                    </Grid>
                </Border>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
                    <Button x:Name="BackButton" Content="{x:Static props:Strings.BackButton}" 
                            Style="{StaticResource PrimaryButtonStyle}" 
                            Width="200" Height="60" Margin="0,0,20,0" 
                            Click="BackButton_Click"/>

                    <Button x:Name="JoinButton" Content="{x:Static props:Strings.JoinMatchButton}" 
                            Style="{StaticResource GreenButtonStyle}" 
                            Width="300" Height="60" 
                            Click="JoinButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>