<UserControl x:Class="GameClient.Views.Components.GameAlertBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">

    <Grid Background="#99000000" Visibility="{Binding Visibility, RelativeSource={RelativeSource AncestorType=UserControl}}">

        <Border Width="500" Height="280" 
                CornerRadius="8" 
                BorderThickness="3" 
                BorderBrush="#2D1B0E"
                VerticalAlignment="Center" 
                HorizontalAlignment="Center">

            <Border.Effect>
                <DropShadowEffect BlurRadius="25" ShadowDepth="10" Opacity="0.6"/>
            </Border.Effect>

            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#5D4037" Offset="0.0"/>
                    <GradientStop Color="#8D6E63" Offset="0.2"/>
                    <GradientStop Color="#6D4C41" Offset="0.5"/>
                    <GradientStop Color="#8D6E63" Offset="0.8"/>
                    <GradientStop Color="#4E342E" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>

            <Grid>
                <Border BorderBrush="#8D6E63" BorderThickness="2" CornerRadius="6" Margin="4" Opacity="0.5"/>

                <Border Width="12" Height="12" Background="#FFD700" CornerRadius="10" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="1" BlurRadius="2"/>
                    </Border.Effect>
                </Border>
                <Border Width="12" Height="12" Background="#FFD700" CornerRadius="10" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="1" BlurRadius="2"/>
                    </Border.Effect>
                </Border>
                <Border Width="12" Height="12" Background="#FFD700" CornerRadius="10" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="1" BlurRadius="2"/>
                    </Border.Effect>
                </Border>
                <Border Width="12" Height="12" Background="#FFD700" CornerRadius="10" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="1" BlurRadius="2"/>
                    </Border.Effect>
                </Border>

                <Grid Margin="25">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="TitleText" 
                               Text="¡ATENCIÓN!" 
                               FontFamily="Segoe UI" 
                               FontSize="26" 
                               FontWeight="Bold" 
                               Foreground="#FFECB3" 
                               HorizontalAlignment="Center">
                        <TextBlock.Effect>
                            <DropShadowEffect ShadowDepth="2" BlurRadius="2" Color="Black"/>
                        </TextBlock.Effect>
                    </TextBlock>

                    <Rectangle Grid.Row="0" Height="2" Width="400" Fill="#3E2723" VerticalAlignment="Bottom" Margin="0,10,0,0" Opacity="0.5"/>

                    <TextBlock x:Name="MessageText" 
                               Grid.Row="1"
                               Text="Aquí va el mensaje de error o notificación del sistema..." 
                               FontSize="18" 
                               Foreground="White" 
                               TextWrapping="Wrap" 
                               TextAlignment="Center" 
                               VerticalAlignment="Center"
                               Margin="0,10">
                        <TextBlock.Effect>
                            <DropShadowEffect ShadowDepth="1" BlurRadius="1" Color="Black" Opacity="0.8"/>
                        </TextBlock.Effect>
                    </TextBlock>

                    <Button x:Name="OkButton"
                            Grid.Row="2"
                            Content="ACEPTAR"
                            Width="150" Height="45"
                            Foreground="White"
                            FontSize="18" FontWeight="Bold"
                            Cursor="Hand"
                            BorderThickness="2"
                            BorderBrush="#3E2723"
                            Click="OkButton_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                        <Button.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#4E342E" Offset="0"/>
                                <GradientStop Color="#3E2723" Offset="1"/>
                            </LinearGradientBrush>
                        </Button.Background>
                        <Button.Effect>
                            <DropShadowEffect ShadowDepth="3" BlurRadius="5" Opacity="0.5"/>
                        </Button.Effect>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>