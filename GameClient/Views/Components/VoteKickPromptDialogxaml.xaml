<UserControl x:Class="GameClient.Views.Dialogs.VoteKickPromptDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">

    <Grid Background="#CC000000">

        <Border Width="400" Height="250" Background="#2D2D30" 
                CornerRadius="15" BorderBrush="#FF4444" BorderThickness="2"
                HorizontalAlignment="Center" VerticalAlignment="Center">

            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Color="Red"/>
            </Border.Effect>

            <StackPanel Margin="20" VerticalAlignment="Center">
                <TextBlock Text="¡VOTACIÓN DE EXPULSIÓN!" 
                           Foreground="#FF4444" FontSize="24" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <TextBlock x:Name="VoteKickTargetText" 
                           Text="¿Expulsar a 'PlayerX'?" 
                           Foreground="White" FontSize="18" 
                           HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"/>

                <TextBlock x:Name="VoteReasonText" 
                           Text="Motivo: ..." 
                           Foreground="Gray" FontSize="14" FontStyle="Italic"
                           HorizontalAlignment="Center" TextWrapping="Wrap" Margin="0,5,0,0"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
                    <Button Content="SÍ (F1)" Width="100" Height="40" Margin="10" 
                            Background="#4CAF50" Foreground="White" FontWeight="Bold"
                            Click="VoteYes_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button Content="NO (F2)" Width="100" Height="40" Margin="10" 
                            Background="#F44336" Foreground="White" FontWeight="Bold"
                            Click="VoteNo_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>